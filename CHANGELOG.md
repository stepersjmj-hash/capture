# 변경 내역

## v1.1.0

- **영역 선택이 기본 도구** (`S`): 캡처 이미지 위에서 드래그하면 Mview 자르기와 같은 마퀴
  (바깥 어둡게 · 행진하는 점선 · 코너 손잡이 · 실시간 `W × H px`)가 잡힌다. `Shift` 정사각형,
  안쪽을 잡아 드래그하면 영역 통째로 이동, `Esc` 선택 지우기.
- **선택 영역 우클릭 메뉴**: `이 영역으로 자르기`(= `Enter`) · `선택 영역 색 채우기` ·
  `선택 영역 테두리 추가` · `선택 지우기`. 자른 뒤에도 얹어 둔 편집 항목은 그대로 따라가고
  `Ctrl+Z` 로 자르기 전으로 되돌아간다.
- **단축키 정리 + `F1` 단축키 보기**: 도구는 선택 `Space` · 영역 `C` · 사각형 `M` · 밑줄 `U` ·
  화살표 `Shift+.` · 텍스트 `T` · 채우기 `F`, 파일은 복사 `Ctrl+C` · 저장 `Ctrl+S` ·
  다른 이름으로 저장 `Ctrl+Shift+S` · 폴더 열기 `Ctrl+F`, 전체 선택 `Ctrl+A`(영역 도구로 바꾸고
  이미지 전체를 선택 — 바로 우클릭해서 채우기·테두리, 되돌리려면 `Esc`), 편집은 되돌리기 `Ctrl+Z` ·
  다시 실행 `Ctrl+Shift+Z`(`Ctrl+Y` 도 됨) · 삭제 `Del`. `F1` 을 누르면 Mview 와 같은
  단축키 치트시트가 뜬다 (아무 곳이나 클릭하거나 `Esc` 로 닫기, 상태바 오른쪽에 `F1 단축키` 안내).
- **캔버스 우클릭 메뉴**: 빈 곳을 우클릭하면 `전체 선택` · 도구 바꾸기(영역 · 사각형 · 밑줄 ·
  화살표 · 텍스트 · 채우기 — 지금 도구에 체크) · `단축키 보기` · `업데이트 확인` ·
  `Mcapture 버전 정보`. 항목 위/선택 영역 안에서는 그 대상에 맞는 메뉴가 그대로 뜬다.
- **텍스트는 기본 테두리 없음**: 외곽선을 빼 글자 색이 그대로 보인다. 필요하면 확정한 텍스트를
  **우클릭 → `테두리`** 로 켜면 되고(다음에 넣을 텍스트에도 이어짐), 같은 메뉴의 `색 변경…` ·
  `텍스트 수정` · `삭제` 로 손볼 수 있다. 사각형·화살표 등 다른 항목도 우클릭하면 색 변경·삭제.

## v1.0.0 — 첫 릴리스

- **트레이 상주 캡처 도구**: 전역 단축키(Windows `Ctrl+Shift+Alt+S`, macOS `⌃⇧⌘S`)로 영역 캡처,
  `…+F` 로 마우스가 있는 화면 전체 캡처. 다중 모니터·고DPI 지원.
- **OS 기본 캡처와 연동**: Windows `Win+Shift+S`, macOS `⌃⇧⌘4` 처럼 결과가 클립보드에 들어오는
  캡처를 감지해 편집 창을 자동으로 연다 (텍스트가 함께 실린 "이미지 복사" 는 무시). 스니핑 도구가 먼저 넣는
  전체 화면 복사본은 걸러 내고 선택한 영역만 연다. 설정에서 끌 수 있음.
- **편집 창**: 사각형 · 밑줄(수평·수직 자동 맞춤, Shift 로 자유 각도) · 화살표 · 텍스트(굵은 글꼴 +
  어두운 외곽선, 확정 후 휠·글자 칸·오른쪽 아래 손잡이로 크기 조절) · 채우기(가리기용). 선택 도구로
  이동·색/굵기 변경·삭제, 텍스트 더블클릭 수정, 되돌리기/다시 실행 100단계.
- **내보내기**: `Ctrl+C` 클립보드 복사, `Ctrl+S` 저장 폴더(기본 `사진\Mcapture`)에 PNG 저장,
  `Ctrl+Shift+S` 다른 이름(PNG/JPEG). 캡처 직후 원본을 클립보드에 바로 복사(설정).
- **설정**: 단축키 변경, 클립보드 감시, 자동 복사, 저장 폴더, Windows 시작 시 자동 실행, 자동 업데이트.
- **자동 업데이트**: 제작자 NAS 에서 새 버전을 읽어 와 교체·재시작 (Windows zip / macOS .app zip).
- 명령줄: `Mcapture --region` / `--full` / `--edit <이미지>` (이미 실행 중이면 그 인스턴스로 전달).
